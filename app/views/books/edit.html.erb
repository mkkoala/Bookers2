<h1>編集画面</h1>
<!-- form_for部分 -->
<%= form_for(@book) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Opinion</h4>
    <%= f.text_area :opinion %>
    <br>
    <%= f.submit 'Update Book' %>
<% end %>
<p><%= link_to 'Show', book_path(@book) %> | Back</p>